# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Node / tooling cruft
node_modules/
npm-debug.log*
yarn-error.log*
package-lock.json
# ...but DO track the server lockfile — `npm ci` in CI/EC2 requires it.
!server/package-lock.json

# Secrets — never commit real env files (templates like *.env.example are fine)
.env
*.env
!*.env.example

# Editor
.vscode/
.idea/
*.swp

# Logs
*.log

# Local test / QA artifacts
.playwright-mcp/
shibka-*.png
